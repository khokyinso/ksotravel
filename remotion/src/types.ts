export interface ScriptProps {
  briefId: string;
  destination: string;
  scriptLines: string[];
  seriesTitle: string;
  triggerPhrase: string;
  brandColor: string;
  footageUrl: string;
  targetDuration: number;
  videoFormat: string;
}
